This package have two major functions
One fuction is install client packages based on a conf file (automate.json)
The other is creating base files (base controllers, models etc) based on template files
